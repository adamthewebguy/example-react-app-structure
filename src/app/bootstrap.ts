/**
 * Application bootstrap entrypoint.
 *
 * Put one-off initialisation here (feature flags, error reporting, analytics,
 * HTTP interceptors, etc). Keep it free of React rendering to make it testable.
 */
export function bootstrapApp(): void {
  // Intentionally empty.
}

