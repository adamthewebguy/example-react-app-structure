/**
 * HTTP interceptors (request/response hooks).
 *
 * Keep transport-wide concerns here: auth headers, retries, logging, etc.
 */
export function applyInterceptors(): void {
  // Stub: implement when you wire a real HTTP client.
}

